name: Daily Scan

on:
  schedule:
    - cron: "0 18 * * *"

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Scan
        run: echo "Run orchestrator backtest scan here"